{
  "name": "dads-gift-fullstack",
  "version": "1.0.0",
  "description": "Dad's Gift - Full Stack Real Estate Platform",
  "scripts": {
    "install:frontend": "cd app && npm ci",
    "install:api": "cd api && npm ci",
    "build": "npm run install:frontend && npm run install:api && cd app && npm run build",
    "dev": "npm run install:frontend && npm run install:api && cd app && npm run dev",
    "start": "cd app && npm run preview"
  },
  "workspaces": [
    "app",
    "api"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}