meta {
  name: Delete a property -Admin only-
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/properties/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
