meta {
  name: Upload multiple images
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/properties/upload
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  images: 
}
