meta {
  name: Add a property to favorites
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/user/favorites
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "propertyId": ""
  }
}
